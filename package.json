{
  "name": "serverless-node-app",
  "version": "1.0.0",
  "description": "Serverless Node.js API with JWT authentication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:frontend": "npm run build && serverless client deploy",
    "deploy:backend": "serverless deploy",
    "deploy:all": "npm run deploy:backend && npm run deploy:frontend",
    "invalidate-cache": "serverless cloudfrontInvalidate"
  },
  "dependencies": {
    "aws-sdk": "^2.1239.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  }
}